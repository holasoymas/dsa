#include <stdbool.h>
#include <stdio.h>

// This Boolean function tests whether the first k
// elements of vector a from a complete solution for the given problem.
bool is_a_solution(int a[], int k, int n) { return k == n; }

int main(void) { return 0; }
